# To run the script with given environment file execute the below command:
## jmeter -n -t email-send.jmx -q local.properties -l result.jtl
# This will generate logs and result.jtl file to track request with parameters value


# To generate report file execute the below command :
## jmeter -g result.jtl -o report-folder
# This will generate a html report of dashboard will all apis characteristics and result



# Generating dynamic request for Multiple Request
PATH=send-email
PORT=8081
HOST=localhost
PROTOCOL=http

# Generating dynamic thread group values
THREAD=3
RAMP_UP_TIME=1
LOOPS=1

